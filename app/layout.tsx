import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'RugPull Detector - Professional Token Analysis',
  description: 'Advanced rugpull detection and risk analysis for new tokens with real-time monitoring and professional analytics.',
  keywords: 'rugpull, token analysis, crypto security, blockchain, risk detection',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
} 